# GoCU GEMM CGO Example

This example demonstrates using GoCU (Go CUDA) with CGO to perform GPU-accelerated matrix multiplication.

## Run

```bash
gox run .
```

## Build

```bash
gox build . -j
```
